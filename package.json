{
  "name": "shorthand",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SHORTHAND MVP - typing action roguelite (spec 1.0 / contract 1.0)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "sim": "tsx scripts/sim.ts",
    "replay": "tsx scripts/replay.ts",
    "e2e": "tsx scripts/e2e.ts",
    "capture": "tsx scripts/capture.ts",
    "deploy": "bash scripts/deploy-pages.sh"
  },
  "dependencies": {
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "playwright": "^1.63.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
